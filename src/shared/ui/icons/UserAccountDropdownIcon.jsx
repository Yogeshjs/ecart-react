import userAccountDropdown from 'images/userAccountDropdown.svg';

export function UserAccountDropdownIcon({ alt, ...restProps }) {
  return <img alt={alt} src={userAccountDropdown} {...restProps} />;
}
